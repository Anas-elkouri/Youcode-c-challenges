#include<stdio.h>

int main()
{
    int a;
    printf("entre x : ");
    scanf("%d",&a);
   int m = 3 * a * a * a * a * a + 2 * a * a * a * a - 5 * a * a *a - a * a + 7 * a - 6; 
    printf("%d",m);
}